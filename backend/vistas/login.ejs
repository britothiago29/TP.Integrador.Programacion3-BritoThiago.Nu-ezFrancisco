<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>FRAP | Login admin</title>
    <link rel="stylesheet" href="/css/estilos.css">
    <style>
/* CONTENEDOR PRINCIPAL */
.login-admin-container {
    max-width: 420px;
    margin: 60px auto;
    padding: 30px;
    background: #ffffff;
    border-radius: 15px;
    box-shadow: 0 0 12px rgba(0,0,0,0.15);
    text-align: center;
    font-family: Arial, sans-serif;
}

/* TÍTULO */
.login-admin-container h2 {
    font-size: 26px;
    margin-bottom: 15px;
}

/* MENSAJE DE ERROR */
.error {
    background: #ffdddd;
    color: #a94442;
    padding: 10px;
    border-left: 4px solid #d9534f;
    border-radius: 5px;
    margin-bottom: 15px;
    font-size: 14px;
    text-align: left;
}

/* FORMULARIO LOGIN */
.login-form {
    display: flex;
    flex-direction: column;
    text-align: left;
}

.login-form label {
    font-size: 14px;
    margin-bottom: 4px;
    font-weight: bold;
}

.login-form input {
    padding: 10px;
    margin-bottom: 12px;
    border-radius: 8px;
    border: 1px solid #ccc;
    font-size: 15px;
    transition: .2s;
}

.login-form input:focus {
    outline: none;
    border-color: #222;
}

/* BOTÓN PRINCIPAL */
.login-form button {
    width: 100%;
    padding: 12px;
    margin-top: 10px;
    background: #222;
    border: none;
    color: white;
    font-size: 16px;
    font-weight: bold;
    border-radius: 10px;
    cursor: pointer;
    transition: .2s;
}

.login-form button:hover {
    background: #444;
}

/* ACCESO RÁPIDO */
.login-fast-form button {
    width: 100%;
    padding: 10px;
    margin-top: 12px;
    background: #3a87ad;
    color: white;
    border: none;
    border-radius: 10px;
    font-size: 15px;
    cursor: pointer;
}

.login-fast-form button:hover {
    background: #337ab7;
}

/* LINK VOLVER */
.login-admin-container a {
    display: inline-block;
    margin-top: 15px;
    text-decoration: none;
    color: #333;
    font-size: 14px;
}

.login-admin-container a:hover {
    text-decoration: underline;
}

/* RESPONSIVE */
@media (max-width: 480px) {
    .login-admin-container {
        max-width: 90%;
        padding: 20px;
    }

    .login-admin-container h2 {
        font-size: 22px;
    }
}
</style>

</head>
<body class="light">
<header class="header">
    <div class="logo-area">
        <img id="logoFrap" src="/assets/imagenes/logo_frap_fclaro.png" class="logo">
    </div>
    <div class="mid_container_light">
        <h3>TP Integrador - Panel Admin</h3>
        <h4>Francisco Núñez Tosi - Thiago Brito</h4>
    </div>
</header>

<main class="login-admin-container">
    <h2>Login administrador</h2>

    <% if (error) { %>
        <p class="error"><%= error %></p>
    <% } %>

    <form method="POST" action="/admin/login" class="login-form">
        <label>Email</label>
        <input type="email" name="email" required>

        <label>Contraseña</label>
        <input type="password" name="password" required>

        <button type="submit">Ingresar</button>
    </form>

    <form method="GET" action="/admin/login/fast" class="login-fast-form">
        <button type="submit">Acceso rápido</button>
    </form>

    <a href="/html/bienvenida.html">Volver a la app</a>
</main>
</body>
</html>
